<Window x:Class="BeTaskManagement.Views.CommentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localHelpers="clr-namespace:BeTaskManagement.Helpers"
        xmlns:mod="clr-namespace:BeTaskManagement.Models.Enums"
        Title="Comment" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Comment Text -->
        <TextBlock Text="Comment:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Text="{Binding CommentText, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0 10 0 0">
            <TextBlock Text="Required By" FontWeight="Bold"/>
            <DatePicker SelectedDate="{Binding ReminderDate}" Width="200" Margin="10 0 0 10"/>
        </StackPanel>

        <!-- Comment Type -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
            <TextBlock Text="Type:" FontWeight="Bold" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Source={localHelpers:EnumHelper {x:Type mod:CommentType}}}" 
                      SelectedItem="{Binding SelectedCommentType}" 
                      Margin="10 0"
                      Width="150"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="80" Margin="0 0 10 0" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>