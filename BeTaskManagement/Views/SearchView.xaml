<Window x:Class="BeTaskManagement.Views.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeTaskManagement.Views"
        mc:Ignorable="d"
        Title="SearchView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Height="30"
                 FontSize="16"
                 Margin="0 0 0 10"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

        <ListView Grid.Row="1"
                  ItemsSource="{Binding FilteredComments}"
                  SelectedItem="{Binding SelectedComment}"
                  MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Comment Text" Width="300" DisplayMemberBinding="{Binding CommentText}"/>
                    <GridViewColumn Header="Type" Width="100" DisplayMemberBinding="{Binding CommentType}"/>
                    <GridViewColumn Header="Date Added" Width="150" DisplayMemberBinding="{Binding DateAdded, StringFormat=dd/MM/yyyy HH:mm}"/>
                    <GridViewColumn Header="Reminder" Width="150" DisplayMemberBinding="{Binding ReminderDate, StringFormat=dd/MM/yyyy HH:mm}"/>
                    <GridViewColumn Header="Task Name" Width="200" DisplayMemberBinding="{Binding BeTask.Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
