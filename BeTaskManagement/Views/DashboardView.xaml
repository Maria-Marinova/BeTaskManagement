<Window x:Class="BeTaskManagement.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeTaskManagement.Views"
        mc:Ignorable="d"
        Title="DashboardView" Height="450" Width="800">
    <ScrollViewer Padding="20">
        <StackPanel>
            <TextBlock Text="📊 Dashboard" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>

            <TextBlock Text="{Binding TotalTasks, StringFormat='Total Tasks: {0}'}" FontSize="16" Margin="0 5"/>
            <TextBlock Text="{Binding OpenTasks, StringFormat='Open: {0}'}" FontSize="16" Margin="0 5"/>
            <TextBlock Text="{Binding InProgressTasks, StringFormat='In Progress: {0}'}" FontSize="16" Margin="0 5"/>
            <TextBlock Text="{Binding ClosedTasks, StringFormat='Closed: {0}'}" FontSize="16" Margin="0 5"/>

            <TextBlock Text="Tasks Due Soon:" FontSize="18" Margin="10 10 0 5"/>
            <ListBox ItemsSource="{Binding TasksDueSoon}" Margin="0 5 0 0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0 5">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding DueDate, StringFormat='Due: {0:dd/MM/yyyy}'}" FontSize="12" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </ScrollViewer>
</Window>
